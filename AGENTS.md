# Workflow Hub — AI 入口

这是多工作流仓库。当前支持 `podcast`，详细规范位于 `workflows/podcast/`。

## 文稿转播客

1. 读取 `workflows/podcast/SCRIPT_SPEC.md`、`presets.yaml` 和对应模板。
2. 在当前对话环境中依据用户材料完成全部口播稿；本仓库不调用大模型，也不改写文稿。保持事实准确，不捏造资料或来源。
3. 每期一个目录 `inputs/podcast/<episode_id>/script.md`。默认新增目录；修改已有文稿前读取原内容。不要改动无关节目。
4. 有 Python 环境时运行 CLI.md 的离线校验。没有执行能力则明确说明尚未校验，不声称验证通过。
5. 上传/提交只保存文稿。仅当用户要求生成音频时，根据 CLI.md 触发 `podcast.yml`。调用 GitHub API 与 CLI 等效，具体使用执行端已授权的能力。
6. `request_id` 使用唯一标识；根据该标识和文稿提交 SHA 找到本次 run，不能把最新任务当成本次任务。记录 run ID 与链接，等待完成，读取状态和产物。
7. 不把任务已排队说成音频已生成，不把校验产物说成 MP3，不把语音服务连接失败说成成功。
8. 默认 `zh-duo-female-male` 或适合用户要求的单人预设。克隆 provider 仅是扩展接口，不支持参考录音克隆，不得假装可用。
9. 本仓库公开，普通文稿不得包含密钥。GitHub/语音服务权限取决于当前插件，只有读取能力时应明确指出不能上传或触发。

新增工作流时保持独立目录，并更新仓库 README 的索引；不要让业务输入上传触发付费或联网生成任务。
